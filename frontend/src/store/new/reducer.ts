import { createSlice } from "@reduxjs/toolkit"


const initialState = {
    news: []
}

const newSlice = createSlice({
    name: 'new',
    initialState,

    reducers: {
        
    },
})

export default newSlice.reducer